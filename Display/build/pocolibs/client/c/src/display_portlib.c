/* --- Generated by genom 2.99.29. Do not edit -------------------------- */

#include "autoconf/acheader.h"

#include "posterLib.h"

#include "display/c/client.h"
#include "serialize.h"
#include "typecopy.h"


/* --- Port genom_state decoding ---------------------------------------- */

int
genom_display_genom_state_decode(
  char *buffer, genom_state_component *dst)
{
  ssize_t len = -1 /* do not check buffer size */;
  char *p = buffer;
  int s;

  s = genom_deserialize_t_genom_state_component(&p, &len, dst);
  if (s) {
    switch(s) {
      case ENOMSG: return S_posterLib_BAD_FORMAT;
      case ENOMEM: return S_posterLib_MALLOC_ERROR;
    }
    return s;
  }
  return 0;
}

void
genom_display_client_genom_state_init_data(
  genom_state_component *data)
{
  genom_tinit_t_genom_state_component(data);
}

void
genom_display_client_genom_state_fini_data(
  genom_state_component *data)
{
  genom_tfini_t_genom_state_component(data);
}


/* --- Port JS decoding ------------------------------------------------- */

int
genom_display_JS_decode(
  char *buffer, display_order *dst)
{
  ssize_t len = -1 /* do not check buffer size */;
  char *p = buffer;
  int s;

  s = genom_deserialize_t_display_order(&p, &len, dst);
  if (s) {
    switch(s) {
      case ENOMSG: return S_posterLib_BAD_FORMAT;
      case ENOMEM: return S_posterLib_MALLOC_ERROR;
    }
    return s;
  }
  return 0;
}

void
genom_display_client_JS_init_data(
  display_order *data)
{
  genom_tinit_t_display_order(data);
}

void
genom_display_client_JS_fini_data(
  display_order *data)
{
  genom_tfini_t_display_order(data);
}

