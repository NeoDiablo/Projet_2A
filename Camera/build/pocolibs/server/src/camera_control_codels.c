/* --- Generated by genom 2.99.29. Do not edit -------------------------- */

#include "autoconf/acheader.h"

#include <signal.h>
#include <unistd.h>

#include "camera_c_types.h"
#include "typecopy.h"
#include "camera_parameters.h"
#include "camera_control_task.h"


/* --- genom_abort_activity_codel ------------------------------------------ */

genom_event
genom_abort_activity_codel(uint32_t activity, genom_context ctx)
{
  if (activity & 0x80000000UL)
    return genom_no_such_activity(ctx);

  /* task film */
  {
    struct genom_component_data *self = ctx->data->self;
    struct genom_activity *a;
    size_t id;

    pthread_mutex_lock(&self->tasks.film.lock);
    for(id = 0; id < genom_max_activities(); id++) {
      a = &self->tasks.film.activities.a[id].any;
      switch(a->status) {
        case ACT_VOID: continue;

        case ACT_INIT: case ACT_RUN:
          if (a->aid != (int32_t)activity) continue;
          a->stop = 1;
          a->interruptedby = "abort_activity";
          pthread_mutex_unlock(&self->tasks.film.lock);
          return genom_ok;

        case ACT_STOP: case ACT_ETHER:
          if (a->aid != (int32_t)activity) continue;
          pthread_mutex_unlock(&self->tasks.film.lock);
          return genom_ok;
      }
    }
    pthread_mutex_unlock(&self->tasks.film.lock);
  }

  return genom_no_such_activity(ctx);
}


/* --- genom_connect_port_codel -------------------------------------------- */

genom_event
genom_connect_port_codel(const char local[128], const char remote[128],
                         genom_context ctx)
{
  struct genom_component_data *self = ctx->data->self;
  genom_event s;
  (void)local; (void)remote; /* fix -Wunused-parameter */

  genom_take_resource(
    self,
    self->resources.task_film ||
    self->resources.all,

    self->resources.all = 1);

  /* find the inport */

  s = genom_no_such_inport(ctx);
  goto done;

done:
  genom_give_resource(self, self->resources.all = 0);
  return s;
}


/* --- genom_connect_remote_codel ------------------------------------------ */

genom_event
genom_connect_remote_codel(const char local[128], const char remote[128],
                           genom_context ctx)
{
  struct genom_component_data *self = ctx->data->self;
  genom_event s;
  (void)local; (void)remote; /* fix -Wunused-parameter */

  genom_take_resource(
    self,
    self->resources.task_film ||
    self->resources.all,

    self->resources.all = 1);

  /* find the remote */

  s = genom_no_such_remote(ctx);
  goto done;

done:
  genom_give_resource(self, self->resources.all = 0);
  return s;
}


/* --- genom_kill_codel ---------------------------------------------------- */

genom_event
genom_kill_codel(genom_context self)
{
  (void)self; /* fix -Wunused-parameter */
  kill(getpid(), SIGTERM);
  return genom_ok;
}


/* --- Service abort_activity control codels ---------------------------- */

genom_event
genom_camera_abort_activity_controlcb(
  struct genom_component_data *self,
  struct genom_camera_abort_activity_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */
  genom_take_resource(
    self,
    self->resources.all,

    self->resources.control = genom_abort_activity_codel);
  s = genom_abort_activity_codel(a->in.activity, &self->control.context);
  genom_give_resource(self, self->resources.control = NULL);

  genom_log_debug("service %s codel %s returned %s",
                  "abort_activity", "genom_abort_activity_codel", s?s:"ok");
  if (s
      && s != genom_incompatible_digest_id
      && s != genom_bad_transition_id
      && s != genom_interrupted_id
      && s != genom_serialization_id
      && s != genom_too_many_activities_id
      && s != genom_disallowed_id
      && s != genom_mwerr_id
      && s != genom_no_such_activity_id
      ) {
    genom_unkex_detail d;
    strncpy(d.what, s, sizeof(d.what)); d.what[sizeof(d.what)-1] = *"";
    genom_log_warn(
      0, "unknown exception %s for codel %s in service %s",
      s, "genom_abort_activity_codel", "abort_activity");
    s = genom_unkex(&d, &self->control.context);
  }
  if (s) return s;

  return s;
}


/* --- Service connect_port control codels ------------------------------ */

genom_event
genom_camera_connect_port_controlcb(
  struct genom_component_data *self,
  struct genom_camera_connect_port_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */
  genom_take_resource(
    self,
    self->resources.all,

    self->resources.control = genom_connect_port_codel);
  s = genom_connect_port_codel(a->in.local, a->in.remote, &self->control.context);
  genom_give_resource(self, self->resources.control = NULL);

  genom_log_debug("service %s codel %s returned %s",
                  "connect_port", "genom_connect_port_codel", s?s:"ok");
  if (s
      && s != genom_incompatible_digest_id
      && s != genom_bad_transition_id
      && s != genom_interrupted_id
      && s != genom_serialization_id
      && s != genom_too_many_activities_id
      && s != genom_disallowed_id
      && s != genom_mwerr_id
      && s != genom_no_such_inport_id
      && s != genom_no_such_outport_id
      && s != genom_port_io_id
      ) {
    genom_unkex_detail d;
    strncpy(d.what, s, sizeof(d.what)); d.what[sizeof(d.what)-1] = *"";
    genom_log_warn(
      0, "unknown exception %s for codel %s in service %s",
      s, "genom_connect_port_codel", "connect_port");
    s = genom_unkex(&d, &self->control.context);
  }
  if (s) return s;

  return s;
}


/* --- Service connect_service control codels --------------------------- */

genom_event
genom_camera_connect_service_controlcb(
  struct genom_component_data *self,
  struct genom_camera_connect_service_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */
  genom_take_resource(
    self,
    self->resources.all,

    self->resources.control = genom_connect_remote_codel);
  s = genom_connect_remote_codel(a->in.local, a->in.remote, &self->control.context);
  genom_give_resource(self, self->resources.control = NULL);

  genom_log_debug("service %s codel %s returned %s",
                  "connect_service", "genom_connect_remote_codel", s?s:"ok");
  if (s
      && s != genom_incompatible_digest_id
      && s != genom_bad_transition_id
      && s != genom_interrupted_id
      && s != genom_serialization_id
      && s != genom_too_many_activities_id
      && s != genom_disallowed_id
      && s != genom_mwerr_id
      && s != genom_no_such_remote_id
      && s != genom_no_such_service_id
      && s != genom_remote_io_id
      ) {
    genom_unkex_detail d;
    strncpy(d.what, s, sizeof(d.what)); d.what[sizeof(d.what)-1] = *"";
    genom_log_warn(
      0, "unknown exception %s for codel %s in service %s",
      s, "genom_connect_remote_codel", "connect_service");
    s = genom_unkex(&d, &self->control.context);
  }
  if (s) return s;

  return s;
}


/* --- Service kill control codels -------------------------------------- */

genom_event
genom_camera_kill_controlcb(
  struct genom_component_data *self,
  struct genom_camera_kill_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */
  genom_take_resource(
    self,
    self->resources.all,

    self->resources.control = genom_kill_codel);
  s = genom_kill_codel(&self->control.context);
  genom_give_resource(self, self->resources.control = NULL);

  genom_log_debug("service %s codel %s returned %s",
                  "kill", "genom_kill_codel", s?s:"ok");
  if (s
      && s != genom_incompatible_digest_id
      && s != genom_bad_transition_id
      && s != genom_interrupted_id
      && s != genom_serialization_id
      && s != genom_too_many_activities_id
      && s != genom_disallowed_id
      && s != genom_mwerr_id
      ) {
    genom_unkex_detail d;
    strncpy(d.what, s, sizeof(d.what)); d.what[sizeof(d.what)-1] = *"";
    genom_log_warn(
      0, "unknown exception %s for codel %s in service %s",
      s, "genom_kill_codel", "kill");
    s = genom_unkex(&d, &self->control.context);
  }
  if (s) return s;

  return s;
}


/* --- Service GetCurrentPosition control codels ------------------------ */

genom_event
genom_camera_GetCurrentPosition_controlcb(
  struct genom_component_data *self,
  struct genom_camera_GetCurrentPosition_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */
  genom_take_resource(
    self,
    self->resources.task_film == InitMoveParameters ||
    self->resources.task_film == FilmPosition ||
    self->resources.all,

    self->resources.control = genom_camera_GetCurrentPosition_controlcb);
  genom_tcopy_t_camera_robot(
    &(a->out.position),
    &(self->ids.position));
  genom_give_resource(self, self->resources.control = NULL);

  /* call simple codels */

  return s;
}


/* --- Service GetCurrentMap control codels ----------------------------- */

genom_event
genom_camera_GetCurrentMap_controlcb(
  struct genom_component_data *self,
  struct genom_camera_GetCurrentMap_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */
  genom_take_resource(
    self,
    self->resources.task_film == InitMoveParameters ||
    self->resources.all,

    self->resources.control = genom_camera_GetCurrentMap_controlcb);
  genom_tcopy_t_camera_map(
    &(a->out.image),
    &(self->ids.image));
  genom_give_resource(self, self->resources.control = NULL);

  /* call simple codels */

  return s;
}


/* --- Service GetCurrentArena control codels --------------------------- */

genom_event
genom_camera_GetCurrentArena_controlcb(
  struct genom_component_data *self,
  struct genom_camera_GetCurrentArena_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */
  genom_take_resource(
    self,
    self->resources.task_film == InitMoveParameters ||
    self->resources.task_film == FilmPosition ||
    self->resources.all,

    self->resources.control = genom_camera_GetCurrentArena_controlcb);
  genom_tcopy_t_camera_arena(
    &(a->out.arene),
    &(self->ids.arene));
  genom_give_resource(self, self->resources.control = NULL);

  /* call simple codels */

  return s;
}


/* --- Service FilmStop control codels ---------------------------------- */

genom_event
genom_camera_FilmStop_controlcb(
  struct genom_component_data *self,
  struct genom_camera_FilmStop_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */

  return s;
}


/* --- Service StartFilm control codels --------------------------------- */

genom_event
genom_camera_StartFilm_controlcb(
  struct genom_component_data *self,
  struct genom_camera_StartFilm_activity *a)
{
  genom_event s = genom_ok;
  (void)self; (void)a; /* fix -Wunused-parameter */

  /* check allowance (before/after statements) */

  /* copy inout parameters to output */

  /* call validate codel */

  /* copy ids parameters to ids (attributes) */

  /* call simple codels */

  return s;
}

/* eof */
