/* --- Generated by genom 2.99.29. Do not edit -------------------------- */

#include "autoconf/acheader.h"

#include "posterLib.h"

#include "camera/c/client.h"
#include "serialize.h"
#include "typecopy.h"


/* --- Port genom_state decoding ---------------------------------------- */

int
genom_camera_genom_state_decode(
  char *buffer, genom_state_component *dst)
{
  ssize_t len = -1 /* do not check buffer size */;
  char *p = buffer;
  int s;

  s = genom_deserialize_t_genom_state_component(&p, &len, dst);
  if (s) {
    switch(s) {
      case ENOMSG: return S_posterLib_BAD_FORMAT;
      case ENOMEM: return S_posterLib_MALLOC_ERROR;
    }
    return s;
  }
  return 0;
}

void
genom_camera_client_genom_state_init_data(
  genom_state_component *data)
{
  genom_tinit_t_genom_state_component(data);
}

void
genom_camera_client_genom_state_fini_data(
  genom_state_component *data)
{
  genom_tfini_t_genom_state_component(data);
}


/* --- Port AR decoding ------------------------------------------------- */

int
genom_camera_AR_decode(
  char *buffer, camera_arena *dst)
{
  ssize_t len = -1 /* do not check buffer size */;
  char *p = buffer;
  int s;

  s = genom_deserialize_t_camera_arena(&p, &len, dst);
  if (s) {
    switch(s) {
      case ENOMSG: return S_posterLib_BAD_FORMAT;
      case ENOMEM: return S_posterLib_MALLOC_ERROR;
    }
    return s;
  }
  return 0;
}

void
genom_camera_client_AR_init_data(
  camera_arena *data)
{
  genom_tinit_t_camera_arena(data);
}

void
genom_camera_client_AR_fini_data(
  camera_arena *data)
{
  genom_tfini_t_camera_arena(data);
}


/* --- Port POS decoding ------------------------------------------------ */

int
genom_camera_POS_decode(
  char *buffer, camera_robot *dst)
{
  ssize_t len = -1 /* do not check buffer size */;
  char *p = buffer;
  int s;

  s = genom_deserialize_t_camera_robot(&p, &len, dst);
  if (s) {
    switch(s) {
      case ENOMSG: return S_posterLib_BAD_FORMAT;
      case ENOMEM: return S_posterLib_MALLOC_ERROR;
    }
    return s;
  }
  return 0;
}

void
genom_camera_client_POS_init_data(
  camera_robot *data)
{
  genom_tinit_t_camera_robot(data);
}

void
genom_camera_client_POS_fini_data(
  camera_robot *data)
{
  genom_tfini_t_camera_robot(data);
}


/* --- Port IM decoding ------------------------------------------------- */

int
genom_camera_IM_decode(
  char *buffer, camera_map *dst)
{
  ssize_t len = -1 /* do not check buffer size */;
  char *p = buffer;
  int s;

  s = genom_deserialize_t_camera_map(&p, &len, dst);
  if (s) {
    switch(s) {
      case ENOMSG: return S_posterLib_BAD_FORMAT;
      case ENOMEM: return S_posterLib_MALLOC_ERROR;
    }
    return s;
  }
  return 0;
}

void
genom_camera_client_IM_init_data(
  camera_map *data)
{
  genom_tinit_t_camera_map(data);
}

void
genom_camera_client_IM_fini_data(
  camera_map *data)
{
  genom_tfini_t_camera_map(data);
}

