/* --- Generated by genom 2.99.29. Do not edit -------------------------- */

#include "autoconf/acheader.h"

#include "portLib.h"
#include "errnoLib.h"
#include "gcomLib.h"

#include "loco/c/client.h"
#include "private.h"
#include "serialize.h"
#include "typecopy.h"
#include "loco_msglib.h"


/* --- Service abort_activity encoding/decoding ------------------------- */

int
genom_loco_abort_activity_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  char *p = dst;
  struct genom_loco_abort_activity_input *in =
    (struct genom_loco_abort_activity_input *)buffer;
  ssize_t s;
  (void)size; /* fix -Wunused-parameter */

  s = 0;
  s += genom_serialen_unsigned_long(
    in->activity);
  if (s > maxsize) return ERROR;

  genom_serialize_unsigned_long(
    &p, in->activity);

  return s;
}

int
genom_loco_abort_activity_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_no_such_activity_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_no_such_activity_id);
    a->report = genom_no_such_activity_id;
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR);
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED);
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_loco_client_abort_activity_init_input(
  struct genom_loco_abort_activity_input *in)
{
  genom_tinit_unsigned_long(
    &(in->activity));

  /* set default value */
  genom_loco_client_abort_activity_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_loco_client_abort_activity_init_output(
  struct genom_loco_abort_activity_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_loco_client_abort_activity_fini_input(
  struct genom_loco_abort_activity_input *in)
{
  genom_tfini_unsigned_long(
    &(in->activity));
}

void
genom_loco_client_abort_activity_fini_output(
  struct genom_loco_abort_activity_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service connect_port encoding/decoding --------------------------- */

int
genom_loco_connect_port_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  char *p = dst;
  struct genom_loco_connect_port_input *in =
    (struct genom_loco_connect_port_input *)buffer;
  ssize_t s;
  (void)size; /* fix -Wunused-parameter */

  s = 0;
  s += genom_serialen_string128(
    in->local);
  s += genom_serialen_string128(
    in->remote);
  if (s > maxsize) return ERROR;

  genom_serialize_string128(
    &p, in->local);
  genom_serialize_string128(
    &p, in->remote);

  return s;
}

int
genom_loco_connect_port_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_no_such_inport_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_no_such_inport_id);
    a->report = genom_no_such_inport_id;
  } else if (!strcmp(p, genom_no_such_outport_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_no_such_outport_id);
    a->report = genom_no_such_outport_id;
  } else if (!strcmp(p, genom_port_io_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_port_io_id);
    a->report = genom_port_io_id;
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR);
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED);
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_loco_client_connect_port_init_input(
  struct genom_loco_connect_port_input *in)
{
  genom_tinit_string128(
    in->local);
  genom_tinit_string128(
    in->remote);

  /* set default value */
  genom_loco_client_connect_port_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_loco_client_connect_port_init_output(
  struct genom_loco_connect_port_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_loco_client_connect_port_fini_input(
  struct genom_loco_connect_port_input *in)
{
  genom_tfini_string128(
    in->local);
  genom_tfini_string128(
    in->remote);
}

void
genom_loco_client_connect_port_fini_output(
  struct genom_loco_connect_port_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service connect_service encoding/decoding ------------------------ */

int
genom_loco_connect_service_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  char *p = dst;
  struct genom_loco_connect_service_input *in =
    (struct genom_loco_connect_service_input *)buffer;
  ssize_t s;
  (void)size; /* fix -Wunused-parameter */

  s = 0;
  s += genom_serialen_string128(
    in->local);
  s += genom_serialen_string128(
    in->remote);
  if (s > maxsize) return ERROR;

  genom_serialize_string128(
    &p, in->local);
  genom_serialize_string128(
    &p, in->remote);

  return s;
}

int
genom_loco_connect_service_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_no_such_remote_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_no_such_remote_id);
    a->report = genom_no_such_remote_id;
  } else if (!strcmp(p, genom_no_such_service_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_no_such_service_id);
    a->report = genom_no_such_service_id;
  } else if (!strcmp(p, genom_remote_io_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_remote_io_id);
    a->report = genom_remote_io_id;
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR);
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED);
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_loco_client_connect_service_init_input(
  struct genom_loco_connect_service_input *in)
{
  genom_tinit_string128(
    in->local);
  genom_tinit_string128(
    in->remote);

  /* set default value */
  genom_loco_client_connect_service_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_loco_client_connect_service_init_output(
  struct genom_loco_connect_service_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_loco_client_connect_service_fini_input(
  struct genom_loco_connect_service_input *in)
{
  genom_tfini_string128(
    in->local);
  genom_tfini_string128(
    in->remote);
}

void
genom_loco_client_connect_service_fini_output(
  struct genom_loco_connect_service_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service kill encoding/decoding ----------------------------------- */

int
genom_loco_kill_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_loco_kill_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR);
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED);
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_loco_client_kill_init_input(
  struct genom_loco_kill_input *in)
{

  /* set default value */
  genom_loco_client_kill_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_loco_client_kill_init_output(
  struct genom_loco_kill_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_loco_client_kill_fini_input(
  struct genom_loco_kill_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_loco_client_kill_fini_output(
  struct genom_loco_kill_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service GetCurrentPosition encoding/decoding --------------------- */

int
genom_loco_GetCurrentPosition_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_loco_GetCurrentPosition_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  struct genom_loco_GetCurrentPosition_output *out = a->output;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;
    s = genom_deserialize_t_camera_robot(
      &p, &len, &(out->position));

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR);
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED);
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_loco_client_GetCurrentPosition_init_input(
  struct genom_loco_GetCurrentPosition_input *in)
{

  /* set default value */
  genom_loco_client_GetCurrentPosition_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_loco_client_GetCurrentPosition_init_output(
  struct genom_loco_GetCurrentPosition_output *out)
{
  genom_tinit_t_camera_robot(
    &(out->position));
}

void
genom_loco_client_GetCurrentPosition_fini_input(
  struct genom_loco_GetCurrentPosition_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_loco_client_GetCurrentPosition_fini_output(
  struct genom_loco_GetCurrentPosition_output *out)
{
  genom_tfini_t_camera_robot(
    &(out->position));
}


/* --- Service GetCurrentBattery encoding/decoding ---------------------- */

int
genom_loco_GetCurrentBattery_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_loco_GetCurrentBattery_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  struct genom_loco_GetCurrentBattery_output *out = a->output;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;
    s = genom_deserialize_t_loco_data(
      &p, &len, &(out->batterie));

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR);
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED);
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_loco_client_GetCurrentBattery_init_input(
  struct genom_loco_GetCurrentBattery_input *in)
{

  /* set default value */
  genom_loco_client_GetCurrentBattery_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_loco_client_GetCurrentBattery_init_output(
  struct genom_loco_GetCurrentBattery_output *out)
{
  genom_tinit_t_loco_data(
    &(out->batterie));
}

void
genom_loco_client_GetCurrentBattery_fini_input(
  struct genom_loco_GetCurrentBattery_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_loco_client_GetCurrentBattery_fini_output(
  struct genom_loco_GetCurrentBattery_output *out)
{
  genom_tfini_t_loco_data(
    &(out->batterie));
}


/* --- Service GetCurrentMap encoding/decoding -------------------------- */

int
genom_loco_GetCurrentMap_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_loco_GetCurrentMap_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  struct genom_loco_GetCurrentMap_output *out = a->output;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;
    s = genom_deserialize_t_camera_map(
      &p, &len, &(out->image));

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR);
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED);
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_loco_client_GetCurrentMap_init_input(
  struct genom_loco_GetCurrentMap_input *in)
{

  /* set default value */
  genom_loco_client_GetCurrentMap_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_loco_client_GetCurrentMap_init_output(
  struct genom_loco_GetCurrentMap_output *out)
{
  genom_tinit_t_camera_map(
    &(out->image));
}

void
genom_loco_client_GetCurrentMap_fini_input(
  struct genom_loco_GetCurrentMap_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_loco_client_GetCurrentMap_fini_output(
  struct genom_loco_GetCurrentMap_output *out)
{
  genom_tfini_t_camera_map(
    &(out->image));
}


/* --- Service GetCurrentArena encoding/decoding ------------------------ */

int
genom_loco_GetCurrentArena_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_loco_GetCurrentArena_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  struct genom_loco_GetCurrentArena_output *out = a->output;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;
    s = genom_deserialize_t_camera_arena(
      &p, &len, &(out->arene));

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR);
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED);
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_loco_client_GetCurrentArena_init_input(
  struct genom_loco_GetCurrentArena_input *in)
{

  /* set default value */
  genom_loco_client_GetCurrentArena_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_loco_client_GetCurrentArena_init_output(
  struct genom_loco_GetCurrentArena_output *out)
{
  genom_tinit_t_camera_arena(
    &(out->arene));
}

void
genom_loco_client_GetCurrentArena_fini_input(
  struct genom_loco_GetCurrentArena_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_loco_client_GetCurrentArena_fini_output(
  struct genom_loco_GetCurrentArena_output *out)
{
  genom_tfini_t_camera_arena(
    &(out->arene));
}


/* --- Service MoveStop encoding/decoding ------------------------------- */

int
genom_loco_MoveStop_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_loco_MoveStop_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR);
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED);
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_loco_client_MoveStop_init_input(
  struct genom_loco_MoveStop_input *in)
{

  /* set default value */
  genom_loco_client_MoveStop_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_loco_client_MoveStop_init_output(
  struct genom_loco_MoveStop_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_loco_client_MoveStop_fini_input(
  struct genom_loco_MoveStop_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_loco_client_MoveStop_fini_output(
  struct genom_loco_MoveStop_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service BattStop encoding/decoding ------------------------------- */

int
genom_loco_BattStop_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_loco_BattStop_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR);
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED);
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_loco_client_BattStop_init_input(
  struct genom_loco_BattStop_input *in)
{

  /* set default value */
  genom_loco_client_BattStop_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_loco_client_BattStop_init_output(
  struct genom_loco_BattStop_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_loco_client_BattStop_fini_input(
  struct genom_loco_BattStop_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_loco_client_BattStop_fini_output(
  struct genom_loco_BattStop_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service StartMove encoding/decoding ------------------------------ */

int
genom_loco_StartMove_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_loco_StartMove_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, loco_INVALID_ORDER_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(loco_INVALID_ORDER_id);
    a->report = loco_INVALID_ORDER_id;
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR);
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED);
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_loco_client_StartMove_init_input(
  struct genom_loco_StartMove_input *in)
{

  /* set default value */
  genom_loco_client_StartMove_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_loco_client_StartMove_init_output(
  struct genom_loco_StartMove_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_loco_client_StartMove_fini_input(
  struct genom_loco_StartMove_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_loco_client_StartMove_fini_output(
  struct genom_loco_StartMove_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}


/* --- Service StartBatt encoding/decoding ------------------------------ */

int
genom_loco_StartBatt_encode(
  char *buffer, int size, char *dst, int maxsize)
{
  (void)buffer; (void)size;
  (void)dst; (void)maxsize; /* fix -Wunused-parameter */
  return 0;
}

int
genom_loco_StartBatt_decode(
  char *buffer, int size, char *dst, int maxsize)
{
  struct genom_client_request *a = (struct genom_client_request *)dst;
  ssize_t len = size;
  char *p = buffer;
  int s;
  (void)maxsize; /* fix -Wunused-parameter */
  if (size == 0) return 0;

  s = 0;
  if (*p == *"") {
    p++; len--;
    a->report = genom_ok;

  } else if (!strcmp(p, genom_incompatible_digest_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_incompatible_digest_id);
    a->report = genom_incompatible_digest_id;
    p += sizeof(genom_incompatible_digest_id);
    a->exdetail = malloc(sizeof(genom_incompatible_digest_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_incompatible_digest(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_incompatible_digest;
    s = genom_deserialize_t_genom_incompatible_digest(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_bad_transition_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_bad_transition_id);
    a->report = genom_bad_transition_id;
    p += sizeof(genom_bad_transition_id);
    a->exdetail = malloc(sizeof(genom_bad_transition_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_bad_transition(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_bad_transition;
    s = genom_deserialize_t_genom_bad_transition(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_interrupted_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_interrupted_id);
    a->report = genom_interrupted_id;
    p += sizeof(genom_interrupted_id);
    a->exdetail = malloc(sizeof(genom_interrupted_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_interrupted(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_interrupted;
    s = genom_deserialize_t_genom_interrupted(&p, &len, a->exdetail);
  } else if (!strcmp(p, genom_serialization_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_serialization_id);
    a->report = genom_serialization_id;
  } else if (!strcmp(p, genom_too_many_activities_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_too_many_activities_id);
    a->report = genom_too_many_activities_id;
  } else if (!strcmp(p, genom_disallowed_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_disallowed_id);
    a->report = genom_disallowed_id;
  } else if (!strcmp(p, genom_mwerr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(genom_mwerr_id);
    a->report = genom_mwerr_id;
    p += sizeof(genom_mwerr_id);
    a->exdetail = malloc(sizeof(genom_mwerr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    genom_tinit_t_genom_mwerr(a->exdetail);
    a->exfini = (void (*)(void *))genom_tfini_t_genom_mwerr;
    s = genom_deserialize_t_genom_mwerr(&p, &len, a->exdetail);
  } else if (!strcmp(p, loco_INVALID_DATA_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    len -= sizeof(loco_INVALID_DATA_id);
    a->report = loco_INVALID_DATA_id;
  } else if (!strcmp(p, genom_syserr_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_syserr_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_syserr_id);
    len -= sizeof(genom_syserr_id) + sizeof(genom_syserr_detail);
    a->report = genom_syserr_id;
    memcpy(a->exdetail, p, sizeof(genom_syserr_detail));
  } else if (!strcmp(p, genom_unkex_id)) {
    assert(a->exdetail == NULL && a->exfini == NULL);
    a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    p += sizeof(genom_unkex_id);
    len -= sizeof(genom_unkex_id) + sizeof(genom_unkex_detail);
    a->report = genom_unkex_id;
    memcpy(a->exdetail, p, sizeof(genom_unkex_detail));
  } else {
    genom_unkex_detail *d;
    assert(a->exdetail == NULL && a->exfini == NULL);
    d = a->exdetail = malloc(sizeof(genom_unkex_detail));
    if (!a->exdetail) {
      errnoSet(S_gcomLib_MALLOC_FAILED);
      return ERROR;
    }
    a->report = genom_unkex_id;
    strncpy(d->what, p, sizeof(d->what));
    d->what[sizeof(d->what)-1] = *"";
  }

  if (s) {
    a->report = genom_ok;
    switch(s) {
      case ENOMSG: errnoSet(S_gcomLib_SMALL_DATA_STR);
      case ENOMEM: errnoSet(S_gcomLib_MALLOC_FAILED);
      default: errnoSet(S_gcomLib_NOT_A_LETTER);
      }
    return ERROR;
  }

  if (len != 0) {
    a->report = genom_ok;
    errnoSet(S_gcomLib_SMALL_LETTER);
    return ERROR;
  }
  return size;
}

void
genom_loco_client_StartBatt_init_input(
  struct genom_loco_StartBatt_input *in)
{

  /* set default value */
  genom_loco_client_StartBatt_json_scan(in,
                  "{ }",
                  NULL);
}

void
genom_loco_client_StartBatt_init_output(
  struct genom_loco_StartBatt_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

void
genom_loco_client_StartBatt_fini_input(
  struct genom_loco_StartBatt_input *in)
{
  (void)in; /* fix -Wunused-parameter */
}

void
genom_loco_client_StartBatt_fini_output(
  struct genom_loco_StartBatt_output *out)
{
  (void)out; /* fix -Wunused-parameter */
}

